/**** Start of imports. If edited, may not auto-convert in the playground. ****/
var geometry = 
    /* color: #ffffff */
    /* shown: false */
    ee.Geometry.Polygon(
        [[[7.003083958928165, 45.21869339705463],
          [7.003083958928165, 45.110246992050875],
          [7.219377293889102, 45.110246992050875],
          [7.219377293889102, 45.21869339705463]]], null, false);
/***** End of imports. If edited, may not auto-convert in the playground. *****/


//For NDVI I would need the same time of the year(Preferably)---> Dates: 
var image1 = ee.Image('COPERNICUS/S2_SR_HARMONIZED/' + '20180619T103019_20180619T103559_T32TLR') //Pre_Fire image
                        // .mosaic().clip(geometry) 
                         .select(['B4','B8']); 

//---------------------------------------------------------

// Expression using image band indices to calculate NBR
var bandIndexExp = '(b("B8") - b("B4")) / (b("B8") + b("B4"))';
var NDVI1 = image1.expression(bandIndexExp).rename('NDVI');



var histogram_NDVI = ui.Chart.image.histogram({
  image: bandIndexImg,
  region: geometry2,
  scale: 10,
  maxBuckets: 50,
  minBucketWidth: 0.01,
})
  .setOptions({
    title: 'NDVI Histogram_Postfire',
    hAxis: { title: 'NDVI' },
    vAxis: { title: 'Pixel Count' }
});







//---------------------Test For Categories------------------------------
print(image);
Map.centerObject(geometry);
Map.addLayer(ndvi_classified , ndviVis , 'NDVI1');
Map.addLayer(geometry);
//Map.addLayer(image);

